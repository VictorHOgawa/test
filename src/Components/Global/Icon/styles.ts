// import { RFValue } from "react-native-responsive-fontsize";
// import { styled } from "styled-components/native";

// interface IconProps {
//   width?: number;
//   height?: number;
// }

// export const Icon = styled.Image<IconProps>`
//   width: ${({ width }) => (width ? RFValue(width) : RFValue(12))}px;
//   height: ${({ height }) => (height ? RFValue(height) : RFValue(12))}px;
// `;
